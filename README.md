# Ansible LAMP (Apache + PHP) + MariaDB roles

This repository will contain reusable Ansible roles to install Apache, PHP (from Ondřej Surý PPA), and MariaDB with a relocated data directory under /srv/mysql.

See the add-ansible-roles branch for the full roles and playbook.
